Pseudocode structure:

Find files to be deleted
Grant them permissions.

rename these files to aaa1
move these files aaa* to a "C:\temp_audio_files"

Grant permissions to the FILES audio codec and FOLDERS.
Move the audio FILES and FOLDERS to System32 folder.


Remove all the permissions to the affected folders and files.


Reboot the computer and then, delete the files from "C:\temp_audio_files"